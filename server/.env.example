# Server port
PORT=5000
# Path to Firebase Admin SDK service account JSON (relative to server/)
FIREBASE_SERVICE_ACCOUNT_JSON=config/firebase-service-account.json
# OR paste the entire JSON as a single-line string (for Vercel)
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}
